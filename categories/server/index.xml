<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>Server on jiangxingye&#39;s blog</title>
    <link>https://jiangxingye.github.io/categories/server/</link>
    <description>Recent content in Server on jiangxingye&#39;s blog</description>
    <generator>Hugo</generator>
    <language>zh-CN</language>
    <lastBuildDate>Tue, 17 Sep 2019 03:36:46 +0800</lastBuildDate>
    <atom:link href="https://jiangxingye.github.io/categories/server/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>ubuntu系统下安装cuda和cudnn驱动方法</title>
      <link>https://jiangxingye.github.io/2019/09/17/ubuntu%E7%B3%BB%E7%BB%9F%E4%B8%8B%E5%AE%89%E8%A3%85cuda%E5%92%8Ccudnn%E9%A9%B1%E5%8A%A8%E6%96%B9%E6%B3%95/</link>
      <pubDate>Tue, 17 Sep 2019 03:25:38 +0800</pubDate>
      <guid>https://jiangxingye.github.io/2019/09/17/ubuntu%E7%B3%BB%E7%BB%9F%E4%B8%8B%E5%AE%89%E8%A3%85cuda%E5%92%8Ccudnn%E9%A9%B1%E5%8A%A8%E6%96%B9%E6%B3%95/</guid>
      <description>ubuntu系统下安装cuda和cudnn驱动方法</description>
      <content:encoded><![CDATA[<p>Ubuntu 16.04 系统下安装 cuda 驱动方法一、cuda install cuda 包含nvidia 显卡驱动 ，如果事先安装好了 nvidia 显卡驱动，在安装 cuda的时候提示安装 nvidia 显卡驱动时候选择 no，不用安装显卡驱动。</p>
<p>cuda 下载链接：https://developer.nvidia.com/cuda-toolkit cuda 安装前准备 二、禁用 ubuntu 系统自带的nvidia 第三方驱动：nouveau禁用方法：</p>
<p>编译/etc/modprobe.d/blacklist.conf nano /etc/modprobe.d/blacklist.conf 在配置文件的最后一页贴加如下命令。</p>
<p>blacklist nouveau options nouveau modeset=0 贴加完毕后按ctrl+x 保存 选 y 保存，然后回车即可退出编译界面。</p>
<p>执行命令加载文件：</p>
<p>update-initramfs –u 重启机器 执行：lsmod | grep nouveau 命令，如果屏幕没有输出，说明禁用nouveau 成功。</p>
<p>三、禁用 X-Window 服务首先用命令：service lightdm stop 禁用图形界面 然后用命令 Ctrl-Alt+F1 进入命令行界面，输入用户名和密码登录 安装 cuda 把 cuda 驱动拷贝到根目录下， 然后进入到根目录下面， 记住要 root 权限安装 。用命令:./cuda.xxx.run -no-opengl-libs 安装 进入安装界面，首先阅读安装说明，可以用 ctrl+c 跳过，也可以用空格一个个的看完。一般直接忽略。</p>
<p>进入安装，第一个问题:选择接受 accept，第二个问题:问是否安装 nvidia显卡驱动？</p>
<p>如果事前安装了驱动，可以选择 no，不然后就选者 yes。然后第三个问题及后面的问题都选择 yes。没有 yes/no 选择的用回车默认就可以了。</p>
<p>Cuda 安装完毕。</p>
<p>四、设置环境变量：终端中输入 ：sudo gedit /etc/profile 在打开的文件末尾，添加以下两行。</p>
<p>export PATH=/usr/local/cuda-10.1/bin:$PATH export LD_LIBRARY_PATH=/usr/local/cuda-10.1/lib64:$LD_LIBRARY_PATH保存文件，并重启。因为 source /etc/profile是临时生效，重启电脑才是永久生效。</p>
<p>五、重启电脑，检查上述的环境变量是否设置成功 a、 验证驱动版本敲入 $ cat /proc/driver/nvidia/version 结果显示 NVRM version:</p>
<p>NVIDIA UNIX x86_64 Kernel Module 384.81 Sat Sep 2 02:43:11 PDT 2017 GCC version:</p>
<p>gcc version 5.4.0 20160609 (Ubuntu 5.4.0-6ubuntu1~16.04.5)b、 验证 CUDA Toolkit 敲入 $ nvcc -V 会输出 CUDA 版本信息 如果是这样的：</p>
<p>The program ‘nvcc’ is currently not installed. You can install it by typing:sudo apt-get install nvidia-cuda-toolkit。</p>
<p>可能是环境配置没有成功，请重复上述步骤 7 测试 CUDA 的 Samples 例子 运行下面命令：</p>
<p>cd /usr/local/cuda-9.0/samples/1_Utilities/deviceQuery make./deviceQuery cd /usr/local/cuda-9.0/samples/1_Utilities/bandwidthTest Make ./ bandwidthTest测试成功会显示结果：pass 六、Cuda 安装成功。</p>
<p>Cuda 安装参考文档及一些常用问题参考此链接：</p>
<p><a href="http://blog.csdn.net/qlulibin/article/details/78714596">http://blog.csdn.net/qlulibin/article/details/78714596</a> cuda 安装完后，安装 cudnn 先去 nvidia 官网下载 cudnn，cudnn下载需要注册账号才能安登陆进去下载。网站：https://developer.nvidia.com/cudnn 先登录，然后同意下载，然后选择自己需要 cudnn 下载 点击下载 tar 的压缩包，然厚把压缩包拷贝到系统下，然后进入到对应目录 执行下面命令，开始安装。</p>
<blockquote>
<p>tar -zxvf cudnn-7.5-linux-x64-v5.0-ga.tgz &gt; cp cuda/include/cudnn.h /usr/local/cuda/include/ cp cuda/lib64/libcudnn* &gt; /usr/local/cuda/lib64/ chmod a+r /usr/local/cuda/include/cudnn.h chmod a+r &gt; /usr/local/cuda/lib64/libcudnn* Cudnn 安装完毕。</p>
</blockquote>
<p>备注：cudnn 的版本需要和 cuda 的版本是要在兼容范围内，若果不兼容，会报错的。</p>
]]></content:encoded>
    </item>
    <item>
      <title>how to install os &amp; install NVIDIA Driver</title>
      <link>https://jiangxingye.github.io/2019/09/11/how-to-install-os-install-nvidia-driver/</link>
      <pubDate>Wed, 11 Sep 2019 10:16:49 +0800</pubDate>
      <guid>https://jiangxingye.github.io/2019/09/11/how-to-install-os-install-nvidia-driver/</guid>
      <description>how to install os &amp;amp; install NVIDIA Driver</description>
      <content:encoded><![CDATA[<p>一.安装Ubuntu1.制作启动盘，选择启动盘启动，硬盘分区自己分，然后按步安装就行就行了。</p>
<p>二.修改主机名和root密码vim /etc/hostname 与vim /etc/hosts/(ip域名) sudo passwd root 三.网络配置vim /etc/network/interfaces 自动获取：</p>
<p>auton eno2 iface eno2 inet dhcp 设置静态地址：</p>
<p>auto eno2 iface eno2 inet static address **** netmask 255.255.255.0 gateway：***** 重启网络服务：</p>
<p>1.service network restart 2./etc/init.d/networking restart 3.ifdown / ifup 配置dns vim /etc/resolv.conf 四.NVIDIA驱动安装1.删除旧的驱动：</p>
<p>sudo apt-get purge nvidia* 2.关闭nouveau 查看属性 sudo ls -lh /etc/modprobe.d/blacklist.conf 修改属性 sudo chmod 666 /etc/modprobe.d/blacklist.conf 禁用：</p>
<p>vim /etc/modprobe.d/blacklist.conf 添加：</p>
<p>blacklist vga16fb blacklist nouveau blacklist rivafb blacklist rivatv blacklist nvidiafb options nouveau modeset=0——如果禁用不成功就加上这句，不行就重启 更新：</p>
<p>sudo update-initramfs -u 重启查看：</p>
<p>lsmod | grep nouveau 关闭图形界面：</p>
<p>init 3 或 sudo service lightdw stop 安装 ：</p>
<p>sudo chmod +x NVIDIA-Linux-x86_64-***run sudo sh NVIDIA-Linux-x86_64-xxx.run -no-x-check -no-nouveau-check -no-opengl-files或sudo ./NVIDIA-Linux-x86_64-xxx.run -no-x-check -no-nouveau-check -no-opengl-files(避免出现循环登录) 安装gcc g++ make（不然可能报错） apt-get install gcc apt-get install g++ apt-get install make 查看:</p>
<p>nvidia-smi或nvidia-settings 卸载：</p>
<p>sudo sh ~/NVIDIA-Linux-x86_64-**.run –uninstall 驱动安装启动不了界面问题解决：</p>
<p>把/etc/lightdm/lightdm.conf备份 把/etc/x11/下面的xorg.conf.failsafe文件复制成xorg.conf 关闭集显 五.安装cuda9.1及cudnn7——关闭NetworkManager（不然重启可能进不去命令行）下载runfile文件 关闭图形界面 验证系统是否安装了kernel header和 package development uname -rsudo apt-get install linux-headers-$(uname -r)——可省略的操作 安装：</p>
<p>sudo sh cuda_9.1.85_387.26_linux.run 配置环境：</p>
<p>vim /etc/profile export PATH=/usr/local/cuda-9.1/bin:$PATH export LD_LIBRARY_PATH=/usr/local/cuda-9.1/lib64:$LD_LIBRARY_PATH 不行的话试试：</p>
<p>vim ~/.bashrc $ export PATH=/usr/local/cuda-9.1/bin${PATH:+:{PATH}}$ export LD_LIBRARY_PATH=/usr/local/cuda-9.1/lib64${LD_LIBRARY_PATH:+:{LD_LIBRARY_PATH}} vim /etc/p rofile export PATH=/usr/local/cuda-9.1/bin:$PATH 链接文件：</p>
<p>vim /etc/ld.so.conf.d/cuda.conf /usr/local/cuda/lib64 sudo lsconfig .查看：</p>
<p>nvcc –version cudnn安装——和cuda一个路径 tar -zxvf ** cd cuda/ sudo cp lib64/libcudnn* /usr/local/cuda-9.1/lib64/ sudo cp include/cudnn.h /usr/local/cuda-9.1/include/ 更新网络连接：</p>
<p>cd /usr/local/cuda-9.1/lib64/ chmod +r libcudnn.so.7.1.1 ——主要看版本 sudo ln -sf libcudnn.so.7.1.1 libcudnn.so.7 sudo ln -sf libcudnn.so.7 libcudnn.so 链接文件：</p>
<p>vim /etc/ld.so.conf.d/cuda.conf /usr/local/cuda-9.1/lib64 查看成功安装与否:ls /usr/local/cuda-9.0/lib64/libcudnn* sudo ldconfig 六.安装docker——一定要跟更新apt-get更新apt-get apt-get update 通过HTTP使用存储库：</p>
<p>sudo apt-get install -y apt-transport-https ca-certificates curl software-properties-common——可不用加 添加密钥 curl -fsSL <a href="https://download.docker.com/linux/ubuntu/gpg">https://download.docker.com/linux/ubuntu/gpg</a> | sudo apt-key add - 设置stable存储库 sudo add-apt-repository “deb [arch=amd64] <a href="https://download.docker.com/linux/ubuntu">https://download.docker.com/linux/ubuntu</a> $(lsb_release -cs) stable” 在更新：</p>
<p>sudo apt-get update 安装最新的docker社区版 sudo apt-get install -y docker-ce 测试：</p>
<p>sudo docker run hello-world 列出可用的版本：</p>
<p>$ apt-cache madison docker-ce 下载nvidia-docker源文件：</p>
<p>wget <a href="https://github.com/nvidia/nvidia-docker/releases/download/v1.0.1/nvidia-docker_1.0.1-1_amd.deb">https://github.com/nvidia/nvidia-docker/releases/download/v1.0.1/nvidia-docker_1.0.1-1_amd.deb</a> <a href="https://github.com/NVIDIA/nvidia-docker/releases/download/v1.0.1/nvidia-docker_1.0.1-1_amd64.deb">https://github.com/NVIDIA/nvidia-docker/releases/download/v1.0.1/nvidia-docker_1.0.1-1_amd64.deb</a> ——注：如果下载不了就用优盘拷贝进去 安装nvidia-docker dpck -i nvidia-docker*.deb 验证：</p>
<p>systemctl start docker或 service docker start docker images docker –version docker run hello-world 七.caffe安装依赖库安装：</p>
<p>sudo apt-get install libprotobuf-dev libleveldb-dev libsnappy-dev libopencv-dev libhdf5-serial-dev protobuf-compiler libgflags-dev libgoogle-glog-dev liblmdb-dev libatlas-base-dev git sudo apt-get install –no-install-recommends libboost-all-dev 如果不行可加上这些依赖项：（cmake build-essential libopenblas-dev liblapack-dev） 安装NCCL：</p>
<p>git clone <a href="https://github.com/NVIDIA/nccl.git">https://github.com/NVIDIA/nccl.git</a> cd nccl sudo make install -j8 安装caffe：</p>
<p>wget <a href="https://github.com/NVIDIA/caffe/archive/v0.15.9.tar.gz">https://github.com/NVIDIA/caffe/archive/v0.15.9.tar.gz</a> /opt tar zxvf v0.15.9.tar.g z cd caffe-0.15.9 cp Makefile,config.example Makefile.config vim Makefile.config 取消 USE_CUDNN:=1的注释和取消USE_NCCL:=1的注释 编译：</p>
<p>make all -j12 出现问题：</p>
<p>1.找不到目录caffe hdf5 解决：vim Makefile.config 85行：INCLUDE_DIRS 后面添加 /usr/include/hdf5/serial/ vim Makefile 173行：</p>
<p>LIBRARIES+=中的hdf5_hl和hdf5改为hdf5_serial_hl和hdf5_serial 2.Unsupport***“compute 20“ 解决：因为安装的是cuda9.1所以vim Makefile.config中CUDA_ARCH 删除20和21结尾的两行 测试：</p>
<p>sudo make runtest -j8 验证：</p>
<p>/build/tools***** 最后把caffe的环境变量加入到etc profile vim /etc/profile eaport PATH= /opt/caffe-0.15.9/build/tools :$PATH cudnn.hpp问题：</p>
<p>因为当前版本的caffe的cudnn实现与系统所安装的cudnn的版本不一致引起的。</p>
<p>解决办法：</p>
<p>1.将./include/caffe/util/cudnn.hpp 换成最新版的caffe里的cudnn的实现，即相应的cudnn.hpp. \2. 将./include/caffe/layers里的，所有以cudnn开头的文件，例cudnn_conv_layer.hpp。</p>
<p>都替换成最新版的c affe里的相应的同名文件。</p>
<p>3.将./src/caffe/layer里的，所有以cudnn开头的文件，例如cudnn_lrn_layer.cu，cudnn_pooling_layer.cpp，cudnn_sigmoid_layer.cu。都替换成最新版的caffe里的相应的同名文件。</p>
<p><a href="https://blog.csdn.net/u014696921/article/details/56014899">https://blog.csdn.net/u014696921/article/details/56014899</a> 编译错误：</p>
<p>.build_release/examples/siamese/convert_mnist_siamese_data.bin 解决方法：</p>
<p>首先这个问题确实是opencv的问题，只需要把 Makefile.config里的 #USE_PKG_CONFIG := 这一行前面的#给去掉，然后在他下一行添加 LIBRARIES += glog gflags protobuf leveldb snappy \ ​ lmdb boost_system hdf5_hl hdf5 m \ ​ opencv_core opencv_highgui opencv_imgproc opencv_imgcodecs 保存，然后再编译即可。如果还不可以，试一下把原来的编译内容用make clean清空，再重新编译。如果还不行，那只能靠诸位再继续大胆尝试了。</p>
<p>八.安装Anaconda应用软件安装在/opt目录下：</p>
<p>bash Anaconda……………….. 添加环境：export PATH=………..：/opt/anaconda3/bin:$PATH source /etc/profile 验证:</p>
<p>conda list 配置好PATH后，可以通过which conda或conda –version命令检查是否正确。</p>
<p>输入conda list 就可以查询，你现在安装了哪些库，常用的numpy, scipy名列其中。如果你还有什么包没有安装上，可以运行conda install * 来进行安装， 如果某个包版本不是最新的，运行 conda update * 就可以了。</p>
<p>九.安装tensorflow——目前只有cuda9.0可以匹配tensorflow1.8和1.9，cuda9.1版本太高安装：pip –default-timeout=100 install -U tensorflow***** 如果没有pip执行:</p>
<p>apt-get install python-pip 验证：</p>
<p>1.python 2.import tensorflow 3.import tensorflow as if 4.a = tf.constant(’hello world’) with tf.Session () as sess:</p>
<p>两个空格–print（sess.run(a)） Ubuntu18.04验证：</p>
<p>import tensorflow as tf hello =tf.constant(‘hello,world’)——创建常量 sess =tf.Session()——创建会话 result =sess.run(hello)——执行 sess.close()——关闭会话 print(result)——输出 十.安装VASP详见vsap篇 十一.安装torchpip –default-timeout=100 install torch torchvision 或：pip3 install torch torchvision 如果局限于网速则可以通whl文件离线安装 验证:</p>
<p>ipython import torch 或：import pytorch import torchvision</p>
]]></content:encoded>
    </item>
    <item>
      <title>what is the NVMe</title>
      <link>https://jiangxingye.github.io/2019/09/06/what-is-the-nvme/</link>
      <pubDate>Fri, 06 Sep 2019 01:59:19 +0800</pubDate>
      <guid>https://jiangxingye.github.io/2019/09/06/what-is-the-nvme/</guid>
      <description>what is the NVMe</description>
      <content:encoded><![CDATA[<p>NVMe is what?先来看一下Wikipredia的解释。wikipedia是这样婶儿说滴。</p>
<p>NVM Express（NVMe），或称非易失性内存主机控制器接口规范（英语：Non-Volatile Memory Host Controller Interface Specification，缩写：NVMHCIS），是一个逻辑设备接口规范。它是与AHCI类似的、基于设备逻辑接口的总线传输协议规范（相当于通讯协议中的应用层），用于访问通过PCI Express（PCIe）总线附加的非易失性存储器介质（例如采用闪存的固态硬盘驱动器），虽然理论上不一定要求PCIe总线协议。</p>
<p>NVM代表非易失性存储器（non-volatile memory）的首字母缩略字，这是固态硬盘（SSD）的常见的闪存形式。此规范主要是为基于闪存的存储设备提供一个低延时、内部并发化的原生界面规范，也为现代CPU、计算机平台及相关应用提供原生存储并发化的支持[1]，令主机硬件和软件可以充分利用固态存储设备的并行化存储能力。相比此前机械硬盘驱动器（HDD）时代的AHCI，NVMe/NVMHCI降低了I/O操作等待时间、提升同一时间内的操作数、更大容量的操作队列等。</p>
<p>依托于PCIe总线，NVMe设备可适用于各种支持PCIe总线的物理插槽上，包括标准尺寸的PCIe扩展卡（一般是4个PCIe通道）[2]、采用U.2物理连接界面（SFF-8639）的2.5英寸/3.5英寸标准尺寸固态硬盘驱动器、[3][4]SATA Express总线（兼容于PCIe）的设备、M.2规格扩展卡等。[5] 此规范由“Non-Volatile Memory Host Controller Interface Working Group”（非易失性存储器主机控制器界面工作组）负责管理。</p>
<p>SATA, SAS, PCIe 都是总线规范, 或者叫电气接口SCSI, NVMe 是存储协议NAND, 3D-Xpoint 是介质我们用的比较多的是SATA SSD, PCIe SSD, NVMe SSD, Optane 常用的SATA SSD 对应的存储命令协议是SCSI, PCIe SSD 对应的存储命令协议一般是NVMe. 所以我们常说的 SATA SSD 指的是 NAND SATA/SAS SCSI SSD. PCIe SSD 指的是 NAND PCIe NVMe SSD, NVMe SSD 指的也是 NAND PCIe NVMe SSD. Optane(Intel 产品) 指的是 3D-Xpoint PCIe NVMe SSD 不管是用户态还是内核态, 都需要有对应的存储协议的支持, 所以对应的内核上都要有对应driver, 比如 SCSI 就是 isci, NVMe 就是nvme 这个驱动, 这个驱动做的事情就是封装对应格式的命令包, 传输给SSD上, SCSI 就封装成isci IO命令包, NVMe封装成 NVMe IO命令包. NVMe 一般会有用户态的驱动, spdk 里面就包含. NVMe 使用内核态也可以把硬件跑满, 但是使用SPDK 的话, 因为使用poll 和 SPDK 的大页机制, 可以让负载比较低的时候 IO 延迟更短 所以我们一般常说的PCIe SSD, 一般就是NVMe SSD NVMe SSD的读大概50us, 写大概可以达到10+us. 因为NVMe内部有ram, 这个写入时间是写到NVMe内部ram 的时间, NVMe自带掉电保护, 所以可以认为写入到内部的ram 就落盘了. 这个ram 的大小是具体根据不同厂商不一样 目前更快的是3Dxpoint 介质的optane/AEP 这两个 optane/AEP 都是intel 的产品. optane也是基于PCIe 的NVMe SSD, AEP 走的是内存设备总线, 所以是内存总线的 NVMe SSD。</p>
<p>使用NVMe有啥优势，可以参考超能网的这篇文章。NVMe到底是什么？用它的SSD有啥优势？</p>
]]></content:encoded>
    </item>
    <item>
      <title>server support OS list</title>
      <link>https://jiangxingye.github.io/2019/09/02/server-support-os-list/</link>
      <pubDate>Mon, 02 Sep 2019 12:20:22 +0800</pubDate>
      <guid>https://jiangxingye.github.io/2019/09/02/server-support-os-list/</guid>
      <description>server support OS list</description>
      <content:encoded><![CDATA[<p>PM 说明 邓克武 单路C系列产品（PR1280C/2280C/6280C/1285C/2285C/1280C4/1285C4）桌面级系统不支持WIN7，支持WIN10,企业级系统最低支持Windows 2012系统，不兼容Windows 2008版本。</p>
<p>图站C系列（PT6610C/PR4610C）/P系列(6620P)/W系列(6630W)系列不支持WIN7，只支持WIN10，不支持Windows服务器级操作系统 曾柳雄 双路3/5/7系列（PRX510P/PRX710P/PR2012P/PR2715P/PR2750P/PR6510P）桌面级系统不支持WIN7，支持WIN10,企业级系统最低支持Windows 2012系统，不兼容Windows 2008版本。</p>
<p>李世伟 AI<strong>系列（PR2906P/2908P/4904P/4908P/4910P/4920P/1904PV/4908PV）桌面级不支持WIN7，支持WIN10,上多张显卡不支持Windows Server系列系统</strong> 李海鸥 PLSphere<strong>虚拟化软件当前版本暂不支持vGPU，但可以支持直通模式使用，后续版本会升级支持vGPU特性</strong> 刘磊 双路3/5/7系列（PR4024P/PR4036P/PR2720TP/PR2725TP/PR2740TP/PR2745TP/PR4740TP/PR4785TP）企业级系统最低支持Windows 2012系统，不兼容Windows 2008版本。</p>
<p>邵武 四路Purely 系列（PR2865P）桌面级系统不支持WIN7，支持WIN10；企业级系统最低支持Windows 2012系统，不兼容Windows 2008版本。</p>
<p>符芳裕 工控机系列（NAS-200H/918H/922H/941H/RNB-1402S），企业级系统最低支持Windows 2008系统，桌面系统支持Win7，Win10；（IPC-909C）,企业级系统最低支持Windows 2012系统，桌面系统只支持Win10/64位；网安机器（903B/916B/918S/928S/915S/925S/926G）,企业级系统最低支持Windows 2008系统，桌面系统支持Win7，Win10。</p>
]]></content:encoded>
    </item>
    <item>
      <title>关闭服务器CPU节能模式</title>
      <link>https://jiangxingye.github.io/2019/08/16/%E5%85%B3%E9%97%AD%E6%9C%8D%E5%8A%A1%E5%99%A8cpu%E8%8A%82%E8%83%BD%E6%A8%A1%E5%BC%8F/</link>
      <pubDate>Fri, 16 Aug 2019 09:58:37 +0800</pubDate>
      <guid>https://jiangxingye.github.io/2019/08/16/%E5%85%B3%E9%97%AD%E6%9C%8D%E5%8A%A1%E5%99%A8cpu%E8%8A%82%E8%83%BD%E6%A8%A1%E5%BC%8F/</guid>
      <description>关闭服务器CPU节能模式</description>
      <content:encoded><![CDATA[<p>OS系统总关闭P-state模式 CentOS7.x OS中使用了 IntelP-state ，导致机器BIOS关闭节电设置后，处理器的频率依旧处于频繁跳动状态。通过grub禁用Intel P-state解决OS控制处理器频率问题。操作如下：</p>
<p>使用root账号进入系统打开终端，两种方式， 第一种：右键 –&gt; Open in Terminal 第二种，左上角Applications –&gt; Utilities -&gt; Terminal （蓝色字体部分为需要执行的命令） 在命令行执行lscpu</p>
<p>|grep “Hz”，查看CPU是否处于额定主频（红色框中数值是否相等或非常相近，2.10GHz=2100MHz，图中明显不相等），然后执行下列命令： 将更改脚本放到/tmp/目录下。在终端执行sh change_grub.sh 如果成功，会自动重启，如果失败，会报错”更改失敗，需要手動進行更改”，请联系运维人员 主板BIOS中设置关闭节能模式 BIOS关闭节能，方法如下： 在上一步执行reboot之后会进行重启操作，过程中进入bios系统后 (按Delete键进入) CPU configuration Advanced Power Managerment Configuretion 在Power Technology 中选择Custom &amp;&amp; 返回 3．CPU P state Cotrol -&gt; Disable &amp;&amp; 返回 4.CPU Cstat Cotrol -&gt; Package C State Limit：C0/C1 state &amp;&amp; 返回 5.关闭QPI 总线的节能: advanced –&gt; Chipset Configuration -&gt; North Bridge -&gt;QPI Configuration -&gt; QPI General Configuretion —&gt; LINK L0p /L1enable更改为disable 。 F4 保存退出执行重启 7、重启完成以后输入账户密码进入系统，打开终端，在命令行执行 lscpu |grep“Hz”查看CPU是否处于额定主频（下图中共色框中两个数值是否相等或非常相近，2.10GHz=2100MHz），如是说明修改成功。 change_grub.sh 脚本如下 #!/bin/sh cmd=&ldquo;intel_pstate=disable intel_idle.max_cstate=0 processor.max_cstate=1 idle=poll quiet&rdquo; check=$(cat /etc/default/grub |grep &ldquo;intel_pstate=disable intel_idle.max_cstate=0 processor.max_cstate=1 idle=poll&rdquo; |</p>
<p>wc -l ) if [ $check -eq 0 ] then sed -i &ldquo;/GRUB_CMDLINE_LINUX/ s/quiet/$cmd/g&rdquo; /etc/default/grub else printf &ldquo;the file has been edited at a earlier time&rdquo; exit 0 fi if [ $?</p>
<p>-ne 0 ] then printf &ldquo;faild,please change it by edit the file&rdquo; exit 1 fi grub2-mkconfig -o /boot/grub2/grub.cfg &amp;&amp; reboot</p>
]]></content:encoded>
    </item>
    <item>
      <title>meiyatest</title>
      <link>https://jiangxingye.github.io/2019/08/15/meiyatest/</link>
      <pubDate>Thu, 15 Aug 2019 07:25:06 +0800</pubDate>
      <guid>https://jiangxingye.github.io/2019/08/15/meiyatest/</guid>
      <description>meiyatest</description>
      <content:encoded><![CDATA[<p>美亚CH服务器性能基准测试 硬件配置一览表 型号 内部名称 技术规格 刀片机 还原（配套智能网卡）-低配 高密多节点服务器，每U≥2节点，总高度≤4U/每节点配置:2颗英特尔至强银牌4110 CPU（2.1GHz，8核）/128G DDR4内存/1<em>600G 15K SAS希捷硬盘/按厂商需求配备raid（有些品牌不配raid没法识别硬盘）/至少配备2个千兆网口，1个独立MGMT带外管理口（支持远程KVM功能)/预留至少1个半高半长pcie3.0</em>8网卡的插槽（长167mm，高68mm,厚16mm）/支持CentOS7.3，同时支持更高版本/带导轨/机箱冗余电源/备注功耗 还原（配套智能网卡）-中配 高密多节点服务器，每U≥2节点，总高度≤4U/每节点配置：2颗英特尔至强银牌4114 CPU（2.2GHz，10核）/256G DDR4内存/1<em>600G 15K SAS希捷硬盘/按厂商需求配备raid（有些品牌不配raid没法识别硬盘）/至少配备2个千兆网口，1个独立MGMT带外管理口（支持远程KVM功能/预留至少1个半高半长pcie3.0</em>8网卡的插槽（长167mm，高68mm,厚16mm）/支持CentOS7.3，同时支持更高版本/带导轨/机箱冗余电源/备注功耗 还原（配套智能网卡）-高配 高密多节点服务器，每U≥2节点，总高度≤4U/每节点配置为：2颗英特尔至强金牌6126 CPU（2.6GHz，12核）/512G DDR4内存/1<em>600G 15K SAS希捷硬盘/按厂商需求配备raid（有些品牌不配raid没法识别硬盘）/至少配备2个千兆网口，1个独立MGMT带外管理口（支持远程KVM功能）/预留至少1个半高半长pcie3.0</em>8网卡的插槽（长167mm，高68mm,厚16mm）/支持CentOS7.3，同时支持更高版本/带导轨/机箱冗余电源/备注功耗 索引服务器 索引 2U服务器/2颗英特尔至强银牌4110 CPU（2.1GHz，8核）/64G DDR4/8<em>900G SAS 10000rpm/ Raid0、1、5、6（1G缓存带超级电容）/双万兆和双千兆网口，不配万兆光模块（要求都是Intel的网卡，并且千兆支持支持IGB驱动，igb-3.2.10、igb-3.4.7、igb-5.1.2 这些版本都可以，万兆支持IXGBE驱动）、1个独立带外管理端口（支持远程KVM）/支持CentOS7.3，同时支持更高版本/带导轨，单电源/ 存储服务器 存储 2U服务器/2颗英特尔至强银牌4110 CPU（2.1GHz，8核）/128G DDR4/12</em>8T SATA 7200RPM/Raid0,1,5,6（1G或以上缓存，带超级电容，支持12个硬盘做1组Raid6）/每服务器双万兆和双千兆网口，不配万兆光模块（要求都是Intel的网卡，并且千兆支持支持IGB驱动，igb-3.2.10、igb-3.4.7、igb-5.1.2 这些版本都可以，万兆支持IXGBE驱动），1个独立MGMT带外管理口（支持远程KVM功能）/支持CentOS7.3及以上版本/带导轨，单电源/要求使用希捷企业级热插拔服务器硬盘/ 系统安装要求出厂预安装:做好raid5、划分VD、安装Centos 7.364位、安装图形化界面等操作。（避免现场安装失败问题） 1、刀片机 ：直接安装系统，无需做raid，按厂商需求配备raid（有些品牌不配raid没法识别硬盘）；</p>
<p>2、SOLR :</p>
<p>raid5，划分2个VD （VD0 ：600G VD1 ：剩余空间 ，挂载为/data） 3、HBase :</p>
<p>raid5，划分2个VD （VD0 ：600G VD1 ：16T ，挂载为/data,其余空间不用挂载） 测试环境准备准备工作 以下要求适用于sorl、存储的CentOS V7.3及以上版本的服务器。</p>
<p>操作系统参数 vi /etc/security/limits.conf * hard nofile 1000000 * soft nofile 1000000 * hard nproc 409600 * soft nproc 409600 vi /etc/security/limits.d/20-nproc.conf * soft nproc 409600 echo 0 &gt; /proc/sys/vm/swappiness;</p>
<p>echo vm.swappiness=0 &raquo; /etc/sysctl.conf 设置linux内核参数：</p>
<p>vi /etc/sysctl.conf net.core.somaxconn = 8192 net.ipv4.tcp_max_syn_backlog = 8192 net.ipv4.tcp_synack_retries = 2 net.ipv4.tcp_syn_retries = 2 net.ipv4.tcp_timestamps = 0 net.ipv4.tcp_window_scaling = 1 net.core.rmem_max = 16777216 net.core.wmem_max = 16777216 net.ipv4.tcp_rmem = 4096 87380 16777216 net.ipv4.tcp_wmem = 4096 87380 16777216 kernel.pid_max = 200000 vm.max_map_count = 400000 并执行：</p>
<p>sysctl -p 禁用SELINUX { 或 /etc/sysconfig/selinux disabled } vi /etc/selinux/config SELINUX=disabled 并执行：</p>
<p>setenforce 0 禁用iptables systemctl stop firewalld #关闭防火墙 systemctl disable firewalld #禁止开机启动 HBase:</p>
<p>gcc-c++安装 CentOS7.3 gcc-c++ 将依赖的包放在ee文件夹上传到/opt目录或者从系统盘中安装 cd /opt/ee 执行如下操作：</p>
<p>rpm -ivh glibc-common-2.17-157.el7.x86_64.rpm rpm -ivh kernel-headers-3.10.0-514.el7.x86_64.rpm rpm -ivh libgcc-4.8.5-11.el7.x86_64.rpm rpm -ivh libgcc-4.8.5-11.el7.i686.rpm rpm -ivh glibc-2.17-157.el7.x86_64.rpm rpm -ivh mpfr-3.1.1-4.el7.x86_64.rpm rpm -ivh libgomp-4.8.5-11.el7.x86_64.rpm rpm -ivh nscd-2.17-157.el7.x86_64.rpm rpm -ivh glibc-headers-2.17-157.el7.x86_64.rpm rpm -ivh glibc-devel-2.17-157.el7.x86_64.rpm rpm -ivh libstdc++-4.8.5-11.el7.x86_64.rpm rpm -ivh libstdc++-devel-4.8.5-11.el7.x86_64.rpm rpm -ivh libmpc-1.0.1-3.el7.x86_64.rpm rpm -ivh cpp-4.8.5-11.el7.x86_64.rpm rpm -ivh gcc-4.8.5-11.el7.x86_64.rpm rpm -ivh gcc-c++-4.8.5-11.el7.x86_64.rpm [@more@] gcc -v （查看是否安装成功） [root@localhost ee]# gcc -v Using built-in specs. COLLECT_GCC=gcc COLLECT_LTO_WRAPPER=/usr/libexec/gcc/x86_64-redhat-linux/4.8.5/lto-wrapper Target:</p>
<p>x86_64-redhat-linux Configured with:</p>
<p>../configure –prefix=/usr –mandir=/usr/share/man–infodir=/usr/share/info –with-bugurl=http://bugzilla.redhat.com/bugzilla–enable-bootstrap –enable-shared –enable-threads=posix–enable-checking=release –with-system-zlib –enable-__cxa_atexit–disable-libunwind-exceptions –enable-gnu-unique-object–enable-linker-build-id –with-linker-hash-style=gnu–enable-languages=c,c++,objc,obj-c++,java,fortran,ada,go,lto –enable-plugin–enable-initfini-array –disable-libgcj–with-isl=/builddir/build/BUILD/gcc-4.8.5-20150702/obj-x86_64-redhat-linux/isl-install–with-cloog=/builddir/build/BUILD/gcc-4.8.5-20150702/obj-x86_64-redhat-linux/cloog-install–enable-gnu-indirect-function –with-tune=generic –with-arch_32=x86-64–build=x86_64-redhat-linux Thread model:</p>
<p>posix gcc version 4.8.5 20150623 (Red Hat 4.8.5-11) (GCC) (说明安装成功) JDK安装 cd /usr/local 将jdk1.7.0_25.tar.gz放在/usr/local解压. ln -s /usr/local/jdk1.7.0_25/ /usr/local/java7 vi /etc/profile 添加：</p>
<p>export JAVA_HOME=/usr/local/java7 export PATH=$JAVA_HOME/bin:$PATH export HADOOP_HOME=/usr/local/hadoop export PATH=$HADOOP_HOME/bin:$PATH export HBASE_HOME=/usr/local/hbase export PATH=$HBASE_HOME/bin:$PATH 并执行：</p>
<p>source /etc/profile FIO测试1、fio安装fio-2.1.10.tar.gz解压至/usr/local/ cd /usr/local tar zxvf fio-2.1.10.tar.gz cd fio-2.0.7makemake install fio测试 2、还原机FIO随机写50MB ，bs 2k fio -filename=/dev/sda -direct=1 -iodepth 8 -thread -rw=randwrite-ioengine=psync -bs=2k -size=50MB -numjobs=30 -runtime=300 -group_reporting-name=mytest 随机写1G ，bs 4k fio -filename=/dev/sda -direct=1 -iodepth 8 -thread -rw=randwrite-ioengine=psync -bs=4k -size=1G -numjobs=30 -runtime=300 -group_reporting-name=mytest 顺序读50M ，bs 2k fio -filename=/dev/sda -direct=1 -iodepth 8 -thread -rw=read -ioengine=psync-bs=2k -size=50MB -numjobs=30 -runtime=300 -group_reporting -name=mytest 顺序读1G ，bs 4k fio -filename=/dev/sda -direct=1 -iodepth 8 -thread -rw=read -ioengine=psync-bs=4k -size=1G -numjobs=30 -runtime=300 -group_reporting -name=mytest 3、索引FIO顺序写500MB ，bs 512k fio -filename=/dev/sdb -direct=1 -iodepth 8 -thread -rw=write -ioengine=psync-bs=512K -size=500MB -numjobs=30 -runtime=300 -group_reporting -name=mytest 顺序写1G，bs 1MB fio -filename=/dev/sdb -direct=1 -iodepth 8 -thread -rw=write -ioengine=psync-bs=1MB -size=1G -numjobs=30 -runtime=300 -group_reporting -name=mytest 随机读500MB ，bs 512k fio -filename=/dev/sdb -direct=1 -iodepth 8 -thread -rw=randread -ioengine=psync-bs=512K -size=500MB -numjobs=30 -runtime=300 -group_reporting -name=mytest 随机读1G ，bs 1MB fio -filename=/dev/sdb -direct=1 -iodepth 8 -thread -rw=randread -ioengine=psync-bs=1MB -size=1G -numjobs=30 -runtime=300 -group_reporting -name=mytest 4、存储FIO顺序写500MB ，bs 512k fio -filename=/dev/sdb -direct=1 -iodepth 8 -thread -rw=write -ioengine=psync-bs=512K -size=500MB -numjobs=30 -runtime=300 -group_reporting -name=mytest 顺序写1G，bs 1MB fio -filename=/dev/sdb -direct=1 -iodepth 8 -thread -rw=write -ioengine=psync-bs=1MB -size=1G -numjobs=30 -runtime=300 -group_reporting -name=mytest 随机读500MB ，bs 512k fio -filename=/dev/sdb -direct=1 -iodepth 8 -thread -rw=randread -ioengine=psync-bs=512K -size=500MB -numjobs=30 -runtime=300 -group_reporting -name=mytest 随机读1G ，bs 1MB fio -filename=/dev/sdb -direct=1 -iodepth 8 -thread -rw=randread -ioengine=psync-bs=1MB -size=1G -numjobs=30 -runtime=300 -group_reporting -name=mytest 索引基准测试SOLR安装solrs.tar 解压至/usr/local/ tar xvf solrs.tar solrs_data.tar 解压至/data tar xvf solrs_data.tar 将数据解压至/datascp ip:/home/benchmark_solr.tar /data cd /data tar xvf benchmark_solr.tar solr基准测试启动solr killall java;</p>
<p>/data/benchmark/solrs/yk/bin/startup.sh 清空系统缓存 echo 3 &gt; /proc/sys/vm/drop_caches 验证http://ip:8900/solr/yk/select?q=<em>%3A</em>&amp;wt=json&amp;indent=true确保访问正常，返回如下结果 登录任意一台服务器（如：172.16.20.159，与solr服务器同一网段），安装perl环境（perl为系统盘对应的版本） 将benchmark.tar.gz文件解压到/opt目录后执行如下操作：</p>
<p>cd /opt/benchmark/solr/ 修改benchmark.perl一些参数配置：</p>
<p>forkCount:并发数 测试并发数为：100 queryCount:查询次数 测试并发数为：1000 urlHost：目标主机ip 根据实际SOLR服务器的ip配置 执行benchmark文件：perl benchmark.perl，运行结果：</p>
<p>Req/s:(每秒请求数) Avg:平均耗时 Median:中间耗时 95th:95%的查询耗在2.604范围 99th:99%的查询耗时在5.498范围 Max：最大耗时 存储基准测试1、主机名配置vi /etc/sysconfig/network 并执行hostname HBase-01 2、主机名查询静态表配置 3、snappy安装snappy-1.1.2.tar.gz解压至/opt/下 cd /opt;</p>
<p>tar zxvf snappy-1.1.2.tar.gz cd /snappy-1.1.2 ./configure make make install 4、ssh无密码登陆ssh-keygen -t rsa （回车到底） cat /root/.ssh/id_rsa.pub &raquo; /root/.ssh/authorized_keys 5、hadoop安装 解压hadoop.tar至/usr/local/ tar xvf hadoop.tar ln -s /usr/local/hadoop-1.0.3/ /usr/local/hadoop ln -s /usr/local/hbase-0.94.1/ /usr/local/hbase 6、hadoop启动killall -s 9 java rm -rf /data/hadoop/ hadoop namenode -format start-all.sh 7、hadoop验证 查看http://172.16.20.232:50070/dfshealth.jsp页面，确保访问正常，同时 DeadNodes为0 执行jps命令,确保有以下的进程 9、HBase基准测试测试工具（yscb安装） mkdir -p /opt/benchmark/hbase/;</p>
<p>解压yscb至/opt/benchmark/hbase/ cd /opt/benchmark/hbase/ tar zxvf ycsb-0.1.4.tar.gz 10、HBase启动清空系统缓存:</p>
<p>echo 3 &gt; /proc/sys/vm/drop_caches killall -s 9 java rm -rf /data/hadoop/ hadoop namenode -format start-dfs.sh start-hbase.sh 11、HBase验证 查看http://172.16.20.232:60010/master-status页面，确保访问正常，同时没有DeadRegion Servers 执行jps命令,确保有以下的进程：</p>
<p>12、HBase基准测试创建hbase表 echo “create ‘usertable’,’f1’,’f2’,’f3’”</p>
<p>|hbase shell 查看表是否已经创建 echo list|hbase shell 清空系统缓存: echo 3 &gt; /proc/sys/vm/drop_caches cd /opt/benchmark/hbase/ycsb-0.1.4/bin; 执行插入测试，创建100个线程,插入100w条记录： ./ycsb load hbase -P ../workloads/workloada -p threads=100 -pcolumnfamily=f1 -p recordcount=1000000 -s &gt; load.dat 测试结果存取在load.dat中,查看load.dat有如下结果 grep -v -E ‘UPDATE|CLEANUP’ load.dat|</p>
<p>more Runtime:执行时间(ms) Throughput(ops/sec):吞吐量(每秒操作次数) Operations：操作总数 AverageLatency：平均耗时(us) MinLatency:最小耗时(us) MaxLatency:最大耗时(us) 95thPercentileLatency(ms):95%的执行时间范围 99thPercentileLatency(ms):99%的执行时间范围 执行事务操作(95%的读操作 5%的写操作) 创建100个线程,操作100w次 ./ycsb run hbase -P ../workloads/workloadb -threads 100 -pmeasurementtype=timeseries -p columnfamily=f1 -p operationcount=1000000 -ptimeseries.granularity=2000 &gt; transactions.dat 测试结果存取在transactions.dat中 查看更新操作测试结果 grep UPDATE transactions.dat 查看读取操作测试结果 grep READ transactions.dat NMON监控在做SOLR或HBase基准测试时，使用nmon监控服务器的CPU、内存、磁盘资源使用情况 1、安装nmon 解压nmon至/usr/local/ cd /usr/local tar zxvf nmon.tar.gz 2、修改nmon参数 cd nmon vi nmon_start.sh 修改频率为10秒一次，监控5000次：</p>
<p>3、运行nmon 做基准测试同时，运行nmon，监控测试时的硬件资源使用情况 ./nmon_start.sh 同一目录下会生成nmon文件 4、关掉nmon 基准测试结束时，停掉nmon Killall nmon 5、生成图形化结果 打开nmon analyser v34a.xls，点击Analyse nmon data按键，加载nmon文件，生成图形化结果：</p>
]]></content:encoded>
    </item>
    <item>
      <title>如何在服务器的BIOS中设置上电自启动</title>
      <link>https://jiangxingye.github.io/2019/08/12/%E5%A6%82%E4%BD%95%E5%9C%A8%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%9A%84bios%E4%B8%AD%E8%AE%BE%E7%BD%AE%E4%B8%8A%E7%94%B5%E8%87%AA%E5%90%AF%E5%8A%A8/</link>
      <pubDate>Mon, 12 Aug 2019 06:36:33 +0800</pubDate>
      <guid>https://jiangxingye.github.io/2019/08/12/%E5%A6%82%E4%BD%95%E5%9C%A8%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%9A%84bios%E4%B8%AD%E8%AE%BE%E7%BD%AE%E4%B8%8A%E7%94%B5%E8%87%AA%E5%90%AF%E5%8A%A8/</guid>
      <description>如何在服务器的BIOS中设置上电自启动</description>
      <content:encoded><![CDATA[<p>说明：</p>
<p>1、为什么要实现这种功能，很多时候在民间都基本用普通PC来做小型服务器，公司的私服等等，而这些普通PC在民用电环境中经常会停电，一停就会导致服务器不能自动来电重启，所以这个功能来点开机是必须的。</p>
<p>2、普通PC基本都是以下这些配置方式，但不要和服务器主板做比较，服务器主板有专业的配置选项以及专业的来电自动启动设备。</p>
<p>下面根据不同的BIOS列出相应的设置方法：</p>
<p>1：首先进入BIOS的设置主界面，选择[POWER MANAGEMENT SETUP]，再选择[PWR Lost Resume State]，这一项有三个选择项。</p>
<p>选择[Keep OFF]项，代表停电后再来电时，电脑不会自动启动。</p>
<p>选择[Turn On]项，代表停电后再来电时，电脑会自动启动。</p>
<p>选择的[Last State]，那么代表停电后再来电时，电脑恢复到停电前电脑的状态。断电前如果电脑是处于开机状态，那么来电后就会自动开机。断电前是处于关机状态，那么来电后电脑不会自动开机。</p>
<p>2：首先进入Power Management Setup（电源管理设定）→Power Again（再来电状态），此项决定了开机时意外断电之后，电力供应恢复时系统电源的状态。设定值有：Power Off（保持系统处于关机状态）Power On（保持系统处于开机状态）Last State（恢复到系统断电前的状态）进入挂起/睡眠模式，但若按钮被揿下超过4秒，机器关机。若想来电自动开机把上面的这项改成power on就行了！</p>
<p>3：有的BIOS中[POWER MANAGEMENT SETUP]没有上面说的[PWR Lost Resume State]，可以在[PWRON After PWR-Fail]→[Integrated Peripherals]选项中找到两个选项：ON(打开自动开机)和OFF(关闭自动开机)，设置为OFF即可。不同的主板及BIOS型号相对应的选项会有所不同，但我想应该会差不多，一般都在[POWER MANAGEMENT SETUP]这个选项中可以找到相应的设置选项！</p>
]]></content:encoded>
    </item>
    <item>
      <title>raid丢失系统恢复案例</title>
      <link>https://jiangxingye.github.io/2019/08/12/raid%E4%B8%A2%E5%A4%B1%E7%B3%BB%E7%BB%9F%E6%81%A2%E5%A4%8D%E6%A1%88%E4%BE%8B/</link>
      <pubDate>Mon, 12 Aug 2019 04:31:04 +0800</pubDate>
      <guid>https://jiangxingye.github.io/2019/08/12/raid%E4%B8%A2%E5%A4%B1%E7%B3%BB%E7%BB%9F%E6%81%A2%E5%A4%8D%E6%A1%88%E4%BE%8B/</guid>
      <description>raid丢失系统恢复案例</description>
      <content:encoded><![CDATA[<p>How to fix “No Bootable Device” issue on server?故障现象：</p>
<p>原因分析：客户机器有独立的RAID卡，2块物理磁盘做的RAID0，和客户沟通，RAID卡进去状态不正常，不想重新安装系统。因为上面的环境配置很麻烦。</p>
<p>处理过程：</p>
<p>重新在RAID卡里做同级别RAID的话，不在raid里做clear操作的话，数据不丢失的话，理论上系统是有办法恢复的，如果数据会消失，那就要重做了。经验证，做同级别RAID系统恢复，数据并未消失。</p>
<p>问题解决：</p>
<p>建议：</p>
<p>如果系统上有重要数据的话，还是建议使用RAID1会安全一些。</p>
<p>如果你忙觉得上述案例帮助了您，给个赞哦，当然也可以打赏杯可乐，我就不用喝酱油兑水了,^_^ 么么哒！</p>
<p>服务器购买也可以通过下面的联系方式向我咨询。</p>
]]></content:encoded>
    </item>
  </channel>
</rss>
