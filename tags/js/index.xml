<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>Js on jiangxingye&#39;s blog</title>
    <link>https://jiangxingye.github.io/tags/js/</link>
    <description>Recent content in Js on jiangxingye&#39;s blog</description>
    <generator>Hugo</generator>
    <language>zh-CN</language>
    <lastBuildDate>Mon, 02 Sep 2019 13:02:59 +0800</lastBuildDate>
    <atom:link href="https://jiangxingye.github.io/tags/js/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>[转]浏览器前进/后退缓存（BF Cache）</title>
      <link>https://jiangxingye.github.io/2017/09/21/bf-cache/</link>
      <pubDate>Thu, 21 Sep 2017 07:35:24 +0800</pubDate>
      <guid>https://jiangxingye.github.io/2017/09/21/bf-cache/</guid>
      <description>[转]浏览器前进/后退缓存（BF Cache）</description>
      <content:encoded><![CDATA[<p><a href="https://developer.mozilla.org/en-US/docs/Working_with_BFCache">浏览器前进/后退缓存</a>（Backward/Forward Cache，BF Cache）是指浏览器在前进后退过程中， 会应用更强的缓存策略，表现为 DOM、window、甚至 JavaScript 对象被缓存，以及同步 XHR 也被缓存。</p>
<p>这一现象在移动端浏览器尤为常见，除 Chrome for Android、Android Browser 之外的浏览器基本都会触发。</p>
<p>BF Cache 本来是一项浏览器优化，但在某些情况下（比如前端路由的 Web App）会引起困惑。</p>
<p>本文主要讨论 BF Cache 的行为、如何检测 BF Cache 缓存、以及如何 workaround。</p>
<p>缓存行为BF Cache 是一种浏览器优化，HTML 标准并未指定其如何进行缓存，因此缓存行为是与浏览器实现相关的。</p>
<p>User agents may discard the Document objects of entries other than the current entry that are not referenced from any script, reloading the pages afresh when the user or script navigates back to such pages. This specification does not specify when user agents should discard Document objects and when they should cache them. – Session history and navigation, WHATWG Desktop Chrome：阻塞的资源和同步发出的 XHR 都会被缓存，但不缓存渲染结果。因此可以看到明显的载入过程，此时脚本也会重新执行。</p>
<p>Chrome for Android：有些情况下不会缓存，缓存时与 Desktop Chrome 行为一致。</p>
<p>Desktop Firefox：页面会被 Frozen，定时器会被暂停，DOM、Window、JavaScript 对象会被缓存，返回时页面脚本重新开始运行。</p>
<p>iOS Safari：渲染结果也会被缓存，因此才能支持左右滑动手势来前进/后退。</p>
<p>Desktop Firefox 暂停计时器的行为非常有趣，以下 HTML 中显示一个每秒加一的数字。</p>
<p>当页面导航时就会暂停，返回时继续增加（因此直接使用 setInterval 倒计时不仅不精确，而且不可靠）：</p>
<p><span id="timer-tick"></span> <a href="http://harttle.com">External Link</a> <script> var i = 0 setInterval(() =&gt; document.querySelector(&rsquo;#timer-tick&rsquo;).innerHTML = i++, 1000) </script> pagehide/pageshow 事件会话（Session）中的某一个页面显示/隐藏时，会触发 pagehide 和 pageshow 事件。</p>
<p>这两个事件都有一个 persisted 属性用来指示当前页面是否被 BF Cache 缓存。</p>
<p>因此可以通过 persisted 属性来达到禁用 BF Cache 的效果：</p>
<p>window.onpageshow = function(event) { if (event.persisted) { window.location.reload() } };</p>
<p>注意无论页面是否被缓存 pageshow 总会触发，因此需要检测器 persisted 属性。</p>
<p>另外 pageshow 的时机总是在 load 事件之后。</p>
<p>这一点很容易检测，下面的 pageshow 日志总在 load 之前：</p>
<p>window.addEventListener(&lsquo;pageshow&rsquo;, function () { console.log(&lsquo;on pageshow&rsquo;) }) window.addEventListener(&rsquo;load&rsquo;, function () { console.log(&rsquo;load&rsquo;) }) XHR 缓存同步（阻塞加载的）脚本发出的 XMLHttpRequest 也会被 Chrome 强制缓存， 因此即使在断网的情况下后退到访问过的页面仍然是可以完美渲染的。</p>
<p>如果页面中有这样一段外部脚本：</p>
<p>sendXHR();</p>
<p>function sendXHR () { var xhr = new XMLHttpRequest() xhr.open(&lsquo;GET&rsquo;, &lsquo;/data.json&rsquo;) xhr.onreadystatechange = function () { if (xhr.readyState === XMLHttpRequest.DONE &amp;&amp; xhr.status === 200) { console.log(&lsquo;xhr arrived&rsquo;, xhr.responseText) } } xhr.send() } 超链接跳转后回来，该 xhr 也会被缓存。注意下图中的 XHR 一项 size 为 “from disk cache”：</p>
<p>为了强制发送 xhr，可以将 xhr 改为异步发送，或者加一个不重要的 query。</p>
<p>setTimeout(sendXHR, 1000) 这样就能看到 xhr 真正发送出去了 :) 异步 xhr 缓存时机未经兼容性测试， 还是建议读者使用一个随机产生的 query。</p>
]]></content:encoded>
    </item>
    <item>
      <title>解决iphone下后退不执行js的问题</title>
      <link>https://jiangxingye.github.io/2017/09/21/iphone-bf-no-run-js/</link>
      <pubDate>Thu, 21 Sep 2017 07:25:32 +0800</pubDate>
      <guid>https://jiangxingye.github.io/2017/09/21/iphone-bf-no-run-js/</guid>
      <description>解决iphone下后退不执行js的问题</description>
      <content:encoded><![CDATA[<p>直接上解决方法不论页面是否被缓存，都会触发 pageshow，所以后退后需要执行的方法可以都放在下面事件内：</p>
<p>window.addEventListener(&lsquo;pageshow&rsquo;, function () { console.log(&lsquo;on pageshow&rsquo;) }) 浏览器缓存行为 的详细介绍可以参考：</p>
<p>[转]浏览器前进/后退缓存（BF Cache）</p>
]]></content:encoded>
    </item>
  </channel>
</rss>
